# Copy to .env for local development. Never commit the filled-in file —
# packages/db/.env is git-ignored at the repo root (see .gitignore).
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/bricks_by_bid_dev
